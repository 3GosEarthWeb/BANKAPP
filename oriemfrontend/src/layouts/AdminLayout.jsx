import React from "react";

const AdminLayout = ({ children }) => {
  return (
    <div className="AdminLayout.jsx-layout">
      {/* TODO: Add layout structure, nav, etc. */}
      {children}
    </div>
  );
};

export default AdminLayout;
