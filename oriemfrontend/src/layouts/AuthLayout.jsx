import React from "react";

const AuthLayout = ({ children }) => {
  return (
    <div className="AuthLayout.jsx-layout">
      {/* TODO: Add layout structure, nav, etc. */}
      {children}
    </div>
  );
};

export default AuthLayout;
