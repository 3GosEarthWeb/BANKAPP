import React from "react";

const CustomerLayout = ({ children }) => {
  return (
    <div className="CustomerLayout.jsx-layout">
      {/* TODO: Add layout structure, nav, etc. */}
      {children}
    </div>
  );
};

export default CustomerLayout;
