import React from "react";

const MainLayout = ({ children }) => {
  return (
    <div className="MainLayout.jsx-layout">
      {/* TODO: Add layout structure, nav, etc. */}
      {children}
    </div>
  );
};

export default MainLayout;
