import React from 'react';

/**
 * User preferences (dark mode, language, etc.)
 */
const Settings = () => {
  return (
    <div>
      <h2>Settings</h2>
      <p>User preferences (dark mode, language, etc.)</p>
    </div>
  );
};

export default Settings;
