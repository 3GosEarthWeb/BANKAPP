import React from 'react';

/**
 * List + filtering of all user transactions
 */
const Transactions = () => {
  return (
    <div>
      <h2>Transactions</h2>
      <p>List + filtering of all user transactions</p>
    </div>
  );
};

export default Transactions;
